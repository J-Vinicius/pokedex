import Container from "components/Container";
import TopBar from "components/TopBar";

function Inicio() {
    return (
        <main class="bg-primary h-screen">
            <TopBar />
            <Container />
        </main>
    );
}

export default Inicio;